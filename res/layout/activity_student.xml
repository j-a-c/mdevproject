<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <Button
        android:id="@+id/upcomingButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="56dp"
        android:text="View Upcoming Events"
        android:textAlignment="center" />

    <Button
        android:id="@+id/settingsButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/bcheckinButton"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="39dp"
        android:text="Settings" />

    <Button
        android:id="@+id/friendsButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/settingsButton"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="25dp"
        android:text="Friends" />

    <Button
        android:id="@+id/bcheckinButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/friendsButton"
        android:layout_alignLeft="@+id/friendsButton"
        android:layout_marginBottom="43dp"
        android:text="Checkin" />

    <Button
        android:id="@+id/currentButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/upcomingButton"
        android:layout_below="@+id/upcomingButton"
        android:layout_marginLeft="18dp"
        android:layout_marginTop="36dp"
        android:text="Current Events in Area" />

</RelativeLayout>